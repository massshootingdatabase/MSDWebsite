/*
const express = require('express');
const router = express.Router();

const {create, get} = require('../controllers/person');

router.route('/create').post(create);

router.route('/get').post(get);

module.exports = router;
*/